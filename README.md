# Smart Resume Screener

A resume screening tool that takes in PDF/text resumes and a job description, then uses a combination of regex parsing and LLM analysis to score and rank candidates. It outputs match scores, skill breakdowns, and short justifications explaining why each candidate does or doesn't fit.

The whole thing runs on free-tier LLM APIs (Google Gemini and Groq), so there's no cost involved.

![Python](https://img.shields.io/badge/Python-3.11+-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.115-green)
![License](https://img.shields.io/badge/License-MIT-yellow)

---

## How It Works

The backend processes each resume through a multi-stage pipeline:

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (Vite)                       │
│  Upload Resumes + JD  →  View Ranked Results + Details  │
└──────────────────────────┬──────────────────────────────┘
                           │ REST API
┌──────────────────────────▼──────────────────────────────┐
│                  Backend (FastAPI)                       │
│                                                         │
│  ┌───────────┐  ┌──────────┐  ┌─────────┐  ┌────────┐ │
│  │pdfplumber │→ │  Regex   │→ │   LLM   │→ │ Scorer │ │
│  │ Extract   │  │  Parser  │  │ Analyze │  │ Ranker │ │
│  └───────────┘  └──────────┘  └────┬────┘  └────────┘ │
│                                     │                   │
│  ┌─────────────┐  ┌────────────────▼────────────────┐  │
│  │   SQLite    │  │  Gemini API  /  Groq API        │  │
│  │  Database   │  │  (free tier, structured JSON)   │  │
│  └─────────────┘  └────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

Step by step:

1. **Text extraction** — `pdfplumber` pulls clean text out of uploaded PDFs (also handles plain `.txt` files directly).
2. **Regex parsing** — A heuristic parser segments the resume into sections (experience, education, skills, projects) and matches skills against a curated taxonomy of 600+ technologies.
3. **LLM-based deep extraction** — The parsed text is sent to Google Gemini (or Groq as fallback) for structured JSON extraction of skills, experience entries, education, and projects.
4. **JD parsing** — The job description goes through a similar LLM pass to pull out required skills, preferred skills, experience requirements, and education expectations.
5. **Match analysis** — A final LLM call compares the candidate's profile against the JD requirements. It scores each dimension, identifies matched and missing skills, and writes a short justification.
6. **Ranking** — Candidates are sorted by their weighted overall score and classified as Strong, Moderate, or Weak matches.

---

## Tech Stack

| Layer | Technology | Rationale |
|:------|:-----------|:----------|
| Backend | FastAPI | Async request handling, automatic OpenAPI docs, native Pydantic integration |
| PDF Extraction | pdfplumber | Reliable text extraction with layout awareness |
| Resume Parsing | Regex + heuristics | No ML dependencies needed; instant results using a ~600 skill taxonomy |
| LLM (Primary) | Google Gemini API | Free tier (~1,500 requests/day), supports structured JSON output |
| LLM (Fallback) | Groq API | Free tier (30 RPM), fastest inference available, OpenAI-compatible |
| LLM Client | OpenAI Python SDK | Works with both Gemini and Groq by swapping `base_url` |
| Database | SQLite + aiosqlite | Zero configuration, stores analysis sessions and parsed data |
| Validation | Pydantic v2 | Schema enforcement for API payloads and LLM responses |
| Frontend | Vite + vanilla JS/CSS | Lightweight, no framework overhead |

---

## Scoring

Each candidate gets a score from 0 to 100, computed as a weighted average across four dimensions:

| Dimension | Weight | How it's measured |
|:----------|:-------|:------------------|
| Skills Match | 35% | Proportion of required JD skills found in the resume (exact + semantic) |
| Experience Relevance | 35% | LLM assessment of how well past roles align with the JD |
| Education Alignment | 15% | Whether the candidate's degree level and field match the requirements |
| Project Relevance | 15% | LLM assessment of how relevant their projects are to the role |

Based on the overall score, candidates are classified as:
- **Strong Match** (75 and above) — meets most or all key requirements
- **Moderate Match** (50–74) — partial fit with some notable gaps
- **Weak Match** (below 50) — significant mismatches in skills or experience

### Guarding against hallucination

This was a real concern since the LLM could easily fabricate skills or inflate scores. Here's what the system does to prevent that:

- Every prompt explicitly tells the LLM to extract **only** what's stated in the resume — never infer or guess.
- Missing skills are computed deterministically (`JD required skills - resume matched skills`), not generated by the LLM.
- All LLM output is validated against Pydantic schemas. If the JSON doesn't conform, the call is retried.
- `temperature=0` across all calls to eliminate creative/random responses.

---

## LLM Prompts

All prompts live in `backend/prompts/`. Here are the actual system instructions used:

### Match Analysis (the main scoring prompt)

This is the core prompt that evaluates candidate-JD fit. It's defined in `backend/prompts/match_analysis.py`:

```
You are a senior technical recruiter performing a candidate-job fit analysis.

You will be given:
1. A candidate's parsed resume data (structured JSON)
2. A job description's requirements (structured JSON)
3. Pre-computed similarity scores from semantic matching (if available)

Your task is to produce a detailed, factual assessment of how well the candidate fits the job.

STRICT RULES — FOLLOW EXACTLY:
1. ONLY cite skills, experience, or qualifications that are PRESENT in the candidate's resume data.
2. A skill NOT in the resume is MISSING — do NOT assume the candidate has it, even if it seems likely.
3. Be specific: reference exact skill names, years of experience, project names, and company names from the resume.
4. For each matched skill, note WHERE in the resume it appears (e.g., "listed in skills", "used at Company X", "used in Project Y").
5. Score each dimension from 0 to 100:
   - skills_score: What percentage of required skills does the candidate have?
   - experience_score: How relevant is their work experience to the role's responsibilities?
   - education_score: Does their education meet or exceed the requirements?
   - project_score: How relevant are their projects to the job?
6. overall_score: Weighted average (skills 35%, experience 35%, education 15%, projects 15%).
7. Strengths: List 2-4 specific strengths relevant to THIS role.
8. Weaknesses: List 1-3 specific gaps or missing qualifications. Be concrete, not generic.
9. Justification: Write 3-5 sentences explaining the overall assessment. Be factual, concise, and actionable.
10. classification: "Strong Match" if overall_score >= 75, "Moderate Match" if >= 50, "Weak Match" if < 50.
11. For missing_skills: list ONLY required skills from the job description that are NOT found anywhere in the resume.
12. For matched_skills: provide the required skill, the matching candidate skill, where it appears (evidence), and a similarity score.
```

### Resume Extraction

Defined in `backend/prompts/resume_extract.py`:

```
You are an expert resume parser. Your task is to extract structured information from the raw text of a resume.

CRITICAL RULES:
1. Extract ONLY information that is EXPLICITLY stated in the resume text.
2. If a field is not found in the text, set it to null or an empty array — NEVER guess or infer.
3. Do NOT hallucinate or fabricate any skills, experience, education, or qualifications.
4. For skills: include both explicitly listed skills AND technologies/tools mentioned within experience or project descriptions.
5. For experience entries: extract company name, job title, start and end dates, and a concise description of responsibilities/achievements.
6. For education: extract institution, degree type, field of study, and graduation year.
7. For projects: extract project name, brief description, and technologies used.
8. Estimate total_years_experience by summing the durations of all work experience entries. If dates are missing, set to null.
9. Keep descriptions concise — summarize, don't copy entire paragraphs verbatim.
10. Normalize skill names to their common form (e.g., "ReactJS" → "React", "K8s" → "Kubernetes").
```

### Job Description Parsing

Defined in `backend/prompts/jd_extract.py`:

```
You are a job description analyzer. Your task is to extract structured requirements from a job posting.

RULES:
1. Distinguish clearly between REQUIRED and PREFERRED (nice-to-have) qualifications.
2. If the job description does not explicitly separate required vs. preferred, treat all listed skills as required.
3. Extract individual skills and technologies as separate items (e.g., "React, Node.js, AWS" becomes three separate entries).
4. If minimum years of experience is stated, extract it as a number. If a range is given (e.g., "3-5 years"), use the lower bound.
5. Extract the job title from the posting.
6. List key responsibilities as concise bullet points.
7. Extract education requirements if specified (e.g., "Bachelor's in Computer Science or related field").
8. Do NOT add requirements that are not in the job description.
```

---

## Setup

### What you need

- Python 3.11 or newer
- Node.js 18+ (for the frontend dev server)
- At least one free LLM API key:
  - [Google AI Studio](https://aistudio.google.com/apikey) — no credit card required
  - [Groq Console](https://console.groq.com/keys) — no credit card required

### Backend

```bash
cd backend

# Create and activate a virtual environment
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Set up environment variables
cp .env.example .env
# Open .env and add your GEMINI_API_KEY and/or GROQ_API_KEY

# Start the server
uvicorn main:app --reload --port 8000
```

The API will be available at `http://localhost:8000`, with interactive docs at `http://localhost:8000/docs`.

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Then open `http://localhost:5173` in your browser.

### Trying it out

There are sample resumes in `sample_data/resumes/` and sample job descriptions in `sample_data/job_descriptions/`. Upload a few resumes, paste in a JD, and hit "Analyze" to see the ranked results.

---

## API Endpoints

| Method | Endpoint | Description |
|:-------|:---------|:------------|
| `GET` | `/api/health` | Health check — reports LLM provider status |
| `POST` | `/api/session` | Create a new analysis session |
| `POST` | `/api/session/{id}/resumes` | Upload one or more resume files |
| `POST` | `/api/session/{id}/job-description` | Submit job description text |
| `POST` | `/api/session/{id}/analyze` | Run the full analysis pipeline |
| `GET` | `/api/session/{id}/results` | Retrieve ranked results |
| `DELETE` | `/api/session/{id}` | Delete a session and its data |

### Example usage with cURL

```bash
# Create a session
SESSION=$(curl -s -X POST http://localhost:8000/api/session | jq -r '.session_id')

# Upload a resume
curl -X POST "http://localhost:8000/api/session/$SESSION/resumes" \
  -F "files=@sample_data/resumes/alice_johnson_senior_dev.txt"

# Submit a job description
curl -X POST "http://localhost:8000/api/session/$SESSION/job-description" \
  -H "Content-Type: application/json" \
  -d '{"text": "Senior Software Engineer... 5+ years React, Node.js, AWS..."}'

# Run analysis
curl -X POST "http://localhost:8000/api/session/$SESSION/analyze"

# Get results
curl "http://localhost:8000/api/session/$SESSION/results"
```

---

## Tests

```bash
# From the project root
python -m pytest tests/ -v

# Run a specific test file
python -m pytest tests/test_parser.py -v
```

The test suite covers:
- Text extraction from PDF and TXT formats, including whitespace normalization
- Contact info extraction (email, phone, LinkedIn, GitHub URLs)
- Skill taxonomy matching against the 600+ skill database
- Resume section detection (experience, education, skills, projects)
- Scoring engine behavior — classification boundaries, weighted scoring, and candidate ranking

---

## Project Structure

```
resume-scanner/
├── backend/
│   ├── main.py              # FastAPI app entry point
│   ├── config.py            # Environment and settings
│   ├── requirements.txt
│   ├── Dockerfile
│   ├── models/
│   │   ├── schemas.py       # Pydantic models for API and LLM data
│   │   └── database.py      # Async SQLite operations
│   ├── services/
│   │   ├── extractor.py     # PDF/TXT text extraction (pdfplumber)
│   │   ├── parser.py        # Regex-based resume parser + skill taxonomy
│   │   ├── llm_client.py    # Dual-provider LLM client (Gemini + Groq)
│   │   └── scorer.py        # Scoring engine and LLM orchestration
│   ├── prompts/
│   │   ├── resume_extract.py
│   │   ├── jd_extract.py
│   │   └── match_analysis.py
│   └── api/
│       └── routes.py        # REST API route definitions
├── frontend/
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── src/
│       ├── main.js          # Application logic and UI rendering
│       ├── style.css        # Design system and styles
│       └── api.js           # Backend API client
├── sample_data/
│   ├── resumes/             # 4 sample resumes for testing
│   └── job_descriptions/    # 2 sample job descriptions
└── tests/
    ├── test_extractor.py
    ├── test_parser.py
    └── test_scorer.py
```

---

## Deployment

### Backend (Render — free tier)

1. Push this repo to GitHub
2. Connect the repo on [render.com](https://render.com)
3. Create a Web Service pointing to the `backend/` directory
4. Use the Docker environment on the free plan
5. Set `GEMINI_API_KEY` and/or `GROQ_API_KEY` as environment variables

### Frontend (Vercel — free tier)

1. Connect the repo on [vercel.com](https://vercel.com)
2. Set root directory to `frontend/`
3. Add `VITE_API_URL` pointing to your Render backend URL
4. Deploy

---

## License

MIT
